<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WorkoutTracker.MainPageView"
             xmlns:local="clr-namespace:WorkoutTracker"
             BackgroundColor="{DynamicResource SecondaryColor}">
  <ContentPage.BindingContext>
    <local:MainPageViewModel/>
  </ContentPage.BindingContext>

  <ScrollView>
    <Grid RowSpacing="25" RowDefinitions="Auto,Auto,Auto"
              Padding="{OnPlatform iOS='30,60,30,30', Default='30'}">

      <Label Text="Hello, World!"
             Grid.Row="0"
             SemanticProperties.HeadingLevel="Level1"
             FontSize="32"
             HorizontalOptions="Center" />

      <Button Text="Start a workout"
              FontAttributes="Bold"
              Grid.Row="1"
              Command="{Binding StartWorkoutCmd}"
              HorizontalOptions="Center" />

      <Button Text="Create a workout template"
              FontAttributes="Bold"
              Grid.Row="2"
              Command="{Binding CreateTemplateCmd}"
              HorizontalOptions="Center" />

    </Grid>
  </ScrollView>
</ContentPage>
